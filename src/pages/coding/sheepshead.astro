---
// src/pages/games/sheepshead.astro
import Layout from "../../layouts/Layout.astro";

const pageTitle = "Sheepshead — Play Online";
const gamePath = "/games/Sheepshead/index.html";
---

<Layout title={pageTitle} description="Play Sheepshead Remastered (portrait 9:16) — Unity WebGL.">
  <style>
    .game-wrap {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.25rem;
      margin-inline: auto;
    }
    @media (min-width: 980px) {
      .game-wrap {
        grid-template-columns: 1.1fr .9fr;
        align-items: start;
      }
    }

    .game-title {
      margin: 0;
      font-size: var(--h1-size);
      font-weight: 1000;
      letter-spacing: -0.01em;
      line-height: calc(var(--line) - .15);
      color: var(--text);
    }

    .about-col h2 { margin: .75rem 0 .5rem; }
    .about-col h3 { margin: .75rem 0 .25rem; }
    .about-col p,
    .about-col ul {
      margin: 0 0 1rem 0;
      color: var(--text);
      font-size: var(--body-size);
      line-height: 1.5;
    }
    .about-col ul { padding-left: 1.25rem; }

    .btnrow {
      display: flex;
      flex-wrap: wrap;
      gap: .75rem;
      margin-top: .25rem;
    }

    /* Buttons */
    .btn {
      display: inline-flex;
      align-items: center;
      gap: .5rem;
      padding: .65rem .9rem;
      border-radius: 10px;
      border: 1px solid var(--border);
      background: var(--card);   /* theme-aware background */
      color: var(--text);        /* theme-aware text */
      cursor: pointer;
      text-decoration: none;
    }
    .btn:hover { background: var(--hover); }

    .btn.primary {
      background: var(--text);   /* primary = solid text color */
      color: var(--bg);          /* invert text */
      border-color: var(--text);
    }

    /* H1-style button */
    .btn.h1 {
      font-size: var(--h1-size);
      font-weight: 1000;
      letter-spacing: -0.01em;
      line-height: calc(var(--line) - .15);
      padding: .4rem .8rem; /* keep reasonable padding with large type */
      border-radius: 12px;
    }

    .stage {
      height: min(90dvh, 980px);
      aspect-ratio: 9/16;
      margin: 0 auto;
      position: relative;
      overflow: hidden;
      max-width: 100%;
      background: #000;
      border-radius: 12px;
      border: 1px solid var(--border);
    }
    .stage iframe {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      border: 0;
      display: block;
    }

    .actions {
      display: flex;
      justify-content: flex-end; /* tip removed; align buttons to the right */
      align-items: center;
      gap: .75rem;
      margin-top: .75rem;
    }

    .muted { color: var(--muted); }
  </style>

  <section class="content game-wrap">
    <!-- Left column: About -->
    <div class="about-col">
      <h1 class="game-title">Sheepshead</h1>
      <p>A trick-taking card game of German origin. This is a single player version with computer controlled opponents.</p>

      <h3>How to play</h3>
      <ul>
        <li>Deck: 24 cards — A, K, Q, J, 10, 9 of each suit.</li>
        <li>Partners: the two players who hold the black queens. If you hold both, go
            <em>alone</em> or call <em>First Trick</em> (first non‑you trick winner becomes your partner).</li>
        <li>Trump: all Queens (♣, ♠, ♥, ♦), then all Jacks (♣, ♠, ♥, ♦), then all Diamonds (A, 10, K, 9).</li>
        <li>Other suits (♠ ♣ ♥): follow suit order A, 10, K, 9. You must follow suit if able; otherwise play trump.</li>
        <li>Trick-taking: highest trump wins; if no trump is played, highest of the led suit wins.</li>
        <li>Points per card: A=11, 10=10, K=4, Q=3, J=2, 9=0 (120 total per hand).</li>
        <li>Scoring (quick gist): Queen partners sweep=12; opponents take any trick=6;
            opponents 31–59 (“cutter”)=3; opponents 61–89=6 to opponents; opponents ≥90=9 to opponents.</li>
      </ul>

      <div class="btnrow">
        <button class="btn primary h1" id="btn-fullscreen" type="button">Enter Fullscreen</button>
      </div>
    </div>

    <!-- Right column: Game -->
    <aside>
      <div class="stage">
        <iframe
          id="gameframe"
          src={gamePath}
          allow="fullscreen; gamepad; xr-spatial-tracking"
          scrolling="no"
          loading="eager"
          title="Sheepshead game"
        ></iframe>
      </div>
      <div class="actions">
        <div class="btnrow">
          <button class="btn primary h1" id="btn-reload" type="button">Reload</button>
        </div>
      </div>
    </aside>
  </section>

  <script>
    const frame = document.getElementById("gameframe");
    const fsBtn = document.getElementById("btn-fullscreen");
    const reloadBtn = document.getElementById("btn-reload");

    fsBtn?.addEventListener("click", async () => {
      try {
        if (document.fullscreenElement) {
          await document.exitFullscreen();
          fsBtn.textContent = "Enter Fullscreen";
        } else {
          await frame.requestFullscreen();
          fsBtn.textContent = "Exit Fullscreen";
        }
      } catch (e) { console.error(e); }
    });

    reloadBtn?.addEventListener("click", () => {
      frame.src = frame.src; // hard reload
    });
  </script>
</Layout>

<section class="content home-wrap">
  <!-- spacer to match About -->
  <div class="spacer-md" aria-hidden="true"></div>

  <!-- identical header row (toggle column preserved, glyph hidden) -->
  <button
    class="acc-summary acc-summary--home"
    type="button"
    disabled
    aria-hidden="true"
    tabindex="-1"
  >
    <span class="acc-toggle" aria-hidden="true"></span>
    <span class="acc-title">Zach Scholz</span>
  </button>

  <!-- subtitle aligned to the same text start as the title -->
  <p class="home-sub indent-acc">Navy Veteran</p>
</section>

<style>
  :root{
    /* keep in sync with About */
    --acc-toggle-w: 1.25rem;
    --acc-gap: 0.6rem;
  }

  /* match About’s container behavior */
  .home-wrap{
    display:grid;
    gap:12px;
    justify-items:start;
    align-items:start;
    margin-inline:auto;
  }

  /* same spacer used on About */
  .spacer-md{ height: clamp(24px, 6vh, 64px); width: 100%; }

  /* same header styles */
  .acc-summary{
    appearance:none; -webkit-appearance:none; box-sizing:border-box;
    width:100%; display:flex; align-items:center; gap:var(--acc-gap);
    padding:6px 0; user-select:none; color:var(--muted);
    background:none; border:0; margin:0; cursor:pointer;
  }
  .acc-summary--home{ pointer-events:none; cursor:default; }

  .acc-toggle{
    display:flex; align-items:center; justify-content:center;
    width:var(--acc-toggle-w);
    font-size:calc(var(--h1-size) * 0.8);
    font-weight:1000; line-height:1; color:var(--text); text-align:center;
  }
  /* hide glyph but keep column width */
  .acc-summary--home .acc-toggle::before{ content:''; }

  .acc-title{
    margin:0; font-size:var(--h1-size); font-weight:1000;
    letter-spacing:-0.01em; line-height:calc(var(--line) - .15); color:var(--text);
  }

  /* subtitle starts where the title text starts */
  .indent-acc{ margin-left: calc(var(--acc-toggle-w) + var(--acc-gap)); }
  .home-sub{ margin:.25rem 0 0; color:var(--muted); font-size:clamp(16px, 2.2vw, 18px); }
</style>
